type syncerProps = {
    drawing: Drawing,
    setDrawing: React.Dispatch<React.SetStateAction<Drawing>>
    }

export default function Syncer({drawing, setDrawing}: syncerProps){
    return (
            <div className="syncer">
            </div>
           ); }
